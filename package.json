{
  "name": "debt-to-console-ratio",
  "version": "1.0.0",
  "private": true,
  "description": "Compare national debt estimates with Bitcoin and classic game console prices.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/HeroOfBitcoin/debt-to-console-ratio.git"
  },
  "homepage": "https://heroofbitcoin.github.io/debt-to-console-ratio/",
  "scripts": {
    "prebuild": "node scripts/prepare-dist.mjs",
    "build": "tsc",
    "postbuild": "node scripts/copy-public.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "verify": "npm test",
    "data:update": "node scripts/update-debt-data.mjs",
    "predeploy": "npm test",
    "deploy": "gh-pages -d dist"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^4.9.5"
  }
}
